
extern crate rsignal;
use rsignal::*;

fn main() {
//    let a = 21_f32 % 7.0 + 7.0;
    //let f = 11_f32 % 7.0 + 7.0;
    //let mut s = signal::Signal::new(a, f);
    //s.graph1(|_, _| {});

    //for i in (0..signal::N) {
        //println!("{}", s.sequance[i as usize]);
    //}
    //println!("Math: {}", s.math());
    //println!("Disp: {}", s.disp());
}
