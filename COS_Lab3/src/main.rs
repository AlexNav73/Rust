
extern crate rsignal;
use rsignal::signal::*;
use rsignal::chart::*;

#[allow(dead_code)]
fn print(y: f32) {
    println!("y: {}", y);
}

fn main() {
    //let mut c = Chart::new();
    //c.show_aj_spectors(print);
}
