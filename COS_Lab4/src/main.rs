
extern crate rsignal;
use rsignal::*;

fn main() {
    //let s = signal::Signal::new(10.0, 20.0);
    //algos::median_aliasing(&s, |y| println!("{}", y));

    //let mut a = [10i32; 10];
    //let mut counter = 1;

    //for i in 0..10 { a[i] = counter; counter += 1; }
    //let s = &a[0..6];

    //for tmp in a.windows(3) {
        //let v = tmp.to_vec();
        //println!("{:?}", v);
    //}

    //println!("{:?}", s);
}
